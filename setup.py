from setuptools import setup, find_packages

setup(
    name='regexbuilder',
    version='0.0.1',
    packages=find_packages(),
    install_requires=[],
    author='Pedro Torres',
    description='A utility for building complex regular expressions',
)